{% extends 'base.html.twig' %}

{% block title %}New Vidéo{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row">
    
        <div class="col-12 mb-3">
            <a href="{{ path('video_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
        
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3>Formulaire d'ajout de vidéo</h3>
                </div>
                <div class="card-body">
                    {{ include('video/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}

